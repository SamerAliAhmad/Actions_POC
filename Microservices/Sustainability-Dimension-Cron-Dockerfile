FROM mcr.microsoft.com/dotnet/aspnet:7.0 AS base
WORKDIR /app

FROM mcr.microsoft.com/dotnet/sdk:7.0 AS build

COPY ["Sustainability_Dimension_Cron/", "Sustainability_Dimension_Cron/"]
COPY ["Assets/", "./Assets"]
COPY ["Service_Mesh/", "./Service_Mesh"]

WORKDIR "/Sustainability_Dimension_Cron"

RUN dotnet build "Sustainability_Dimension_Cron.csproj" -c Release -o /app/build

FROM build AS publish
RUN dotnet publish "Sustainability_Dimension_Cron.csproj" -c Release -o /app/publish

FROM base AS final
WORKDIR /app
COPY --from=publish /app/publish .

ENTRYPOINT ["dotnet", "Sustainability_Dimension_Cron.dll"]
